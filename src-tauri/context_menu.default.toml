# Default context-menu layout. Each table lists the actions (in display order) shown when
# right-clicking a given kind of entry. This file is the source of truth loaded at startup;
# a user copy in the app config dir overrides it.
#
# Action ids map to the predefined actions in the app:
#   open               open a file with the OS default app, or enter a folder
#   open_in_new_tab    open a folder in a fresh tab and focus it
#   open_containing_folder  open a Recents/search result's parent in a fresh tab
#   open_in_terminal   open a terminal at the folder (folders / a file's parent)
#   create_file        submenu: create a new file inside a folder
#   preview            in-app preview (images, markdown, pdf, video, audio)
#   copy               copy to the internal clipboard
#   copy_path          copy the entry's filesystem path(s) to the OS clipboard
#   cut                cut to the internal clipboard
#   paste              paste the clipboard into the current directory
#   rename             inline rename
#   trash              move to the system Trash (reversible)
#   restore            restore a trashed item to where it came from (or pick a folder)
#   destroy            permanently delete (irreversible)
#   properties         open the properties panel
#   compress           submenu: compress the selection into an archive (.zip)
#   extract            extract an archive's top-level entries directly beside it ("Extract Here")
#   extract_to_folder  extract an archive into a new subfolder beside it, named after the archive
#   sort_by            submenu: pick the sort key/direction (per folder)
#   toggle_hidden      toggle showing hidden entries / dotfiles (global)
#
# Use the literal "separator" to draw a divider between groups. Order here is display order.

# Right-click on the empty floor of the current directory.
[directory]
actions = [
  "new_folder",
  "create_file",
  "paste",
  "separator",
  "sort_by",
  "toggle_hidden",
  "separator",
  "open_in_terminal",
  "separator",
  "properties",
]

# A sub-folder entry.
[folder]
actions = [
  "open",
  "open_in_new_tab",
  "open_containing_folder",
  "open_in_terminal",
  "create_file",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]

# Any file with no more specific type below (the fallback).
[file]
actions = [
  "open",
  "open_containing_folder",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]

# Any entry (file or folder) while browsing the Trash: it's already trashed, so it offers
# Restore and permanent delete instead of Move-to-Trash. Applies to all kinds in the Trash.
[trash]
actions = [
  "open",
  "open_containing_folder",
  "separator",
  "restore",
  "separator",
  "destroy",
  "separator",
  "properties",
]

# ── File types (matched by extension, case-insensitive) ──
# A file whose extension matches one of these uses that type's actions instead of [file].
# Each adds `preview` to the generic file set.

[file_type.image]
extensions = ["png", "jpg", "jpeg", "webp", "gif"]
actions = [
  "open",
  "preview",
  "open_containing_folder",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]

[file_type.markdown]
extensions = ["md", "markdown"]
actions = [
  "open",
  "preview",
  "open_containing_folder",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]

[file_type.pdf]
extensions = ["pdf"]
actions = [
  "open",
  "preview",
  "open_containing_folder",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]

# Videos open in the OS's default player (no in-app "preview" action — the webview struggles
# with large/long files). Thumbnails still work.
[file_type.video]
extensions = ["mp4", "webm", "mov", "m4v", "ogv"]
actions = [
  "open",
  "open_containing_folder",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]

[file_type.audio]
extensions = ["mp3", "wav", "ogg", "flac", "m4a"]
actions = [
  "open",
  "preview",
  "open_containing_folder",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]

# Archive files: offer extraction actions beside Compress, in place of a preview.
[file_type.archive]
extensions = ["zip", "7z"]
actions = [
  "open",
  "open_containing_folder",
  "separator",
  "copy",
  "copy_path",
  "cut",
  "rename",
  "compress",
  "extract",
  "extract_to_folder",
  "separator",
  "trash",
  "destroy",
  "separator",
  "properties",
]
