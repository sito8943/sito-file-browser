import {
  CUSTOM_ACTION_ICON,
  CUSTOM_ACTION_TARGET,
  type CustomActionIcon,
  type CustomActionTarget,
} from "@/shared/constants";

export const CUSTOM_ACTION_ICON_OPTIONS = Object.values(
  CUSTOM_ACTION_ICON,
) as CustomActionIcon[];

export const CUSTOM_ACTION_TARGET_OPTIONS = Object.values(
  CUSTOM_ACTION_TARGET,
) as CustomActionTarget[];

export const CUSTOM_ACTION_ARGUMENT_ROWS = 4;
