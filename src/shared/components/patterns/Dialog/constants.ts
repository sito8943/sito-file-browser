// Matches the local Dialog.css close transition so @sito/ui keeps its portal mounted until the
// app-specific opacity/scale animation has completed.
export const DIALOG_EXIT_DURATION_MS = 200;
export const DIALOG_INITIAL_FOCUS = "first-input" as const;
export const DIALOG_ID_PREFIX = "sfb-dialog";
export const DIALOG_DRAG_X_CSS_VAR = "--dialog-drag-x";
export const DIALOG_DRAG_Y_CSS_VAR = "--dialog-drag-y";
